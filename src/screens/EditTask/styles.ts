import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 16,
  },
  input: {
    borderColor: '#ccc',
    borderWidth: 1,
    height: 40,
    marginBottom: 16,
    paddingHorizontal: 8,
  },
  label: {
    fontSize: 18,
    marginBottom: 8,
  },
});
